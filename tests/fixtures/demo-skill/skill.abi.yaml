capabilities:
  optional: []
  required:
  - filesystem.read
inputs:
  schema: schemas/in.json
outputs:
  schema: schemas/out.json
skill: demo-skill
spec: sabi/v0.1
verification:
  required:
  - artifact.exists
version: 1.0.0
